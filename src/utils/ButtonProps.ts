export enum ButtonStyles {
    Primary = 'primary',
    Secondary = 'secondary'
}

export enum ButtonSizes {
    Small = 'small',
    Medium = 'medium',
    Large = 'large'
}

export enum ButtonVariants {
    Primary = 'primary',
    Contained = 'contained'
}
